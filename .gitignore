# Secrets — never commit
.env
.env.local
.env.*.local

# Node
node_modules/

# Runtime logs
logs/*.log

# macOS
.DS_Store
Thumbs.db

# Data — exclude raw cache but track the final brief
data/
!data/brief.json
!data/.gitkeep
